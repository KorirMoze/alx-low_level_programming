#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 0
*/
int _islower(int c)
{
	   int c = 'r';

   (c >= 'a' && c <= 'z');
   if( islower(c) ) {
      printf("1" );
   } else {
      printf("0" );
   }
   

   
   return(0);
